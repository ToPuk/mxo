server:
  port: 8080
  error:
    include-message: always

spring:
  application:
    name: MXO
  datasource: 
    url: jdbc:postgresql://localhost:7878/mxo
    username: user
    password: test123
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet

